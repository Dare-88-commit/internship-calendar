<?php

declare(strict_types=1);

$title = $title ?? 'No data available';
$message = $message ?? '';
$buttonText = $buttonText ?? '';
$buttonHref = $buttonHref ?? '#';
?>

<div class="empty-state intern-calendar-empty">
    <h2><?= Wo_Secure($title) ?></h2>
    <p><?= Wo_Secure($message) ?></p>
    <?php if ($buttonText !== ''): ?>
        <a class="btn" href="<?= Wo_Secure($buttonHref) ?>"><?= Wo_Secure($buttonText) ?></a>
    <?php endif; ?>
</div>

