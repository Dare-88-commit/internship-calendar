<?php

declare(strict_types=1);

$wo = $wo ?? [];
$calendar = $wo['internship_calendar'] ?? [];
$errors = $calendar['errors'] ?? [];
$values = $calendar['values'] ?? [
    'week' => '',
    'day' => '',
    'title' => '',
    'description' => '',
    'success_criteria' => '',
    'traps' => '',
    'event_date' => '',
];
?>

<section class="form-shell">
    <div class="section-header">
        <div>
            <span class="pill accent">Create entry</span>
            <h1 class="section-title">Add Internship Event</h1>
            <p class="section-subtitle">Create a new event inside the Wondertag-aligned flow.</p>
        </div>
        <a class="ghost-btn" href="index.php?link1=internship-calendar">Back to calendar</a>
    </div>

    <?php if ($errors): ?>
        <div class="flash flash-error">
            <?php foreach ($errors as $error): ?>
                <div><?= Wo_Secure($error) ?></div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>

    <form method="post" class="panel">
        <div class="form-grid">
            <div class="field">
                <label for="week">Week</label>
                <input type="number" min="1" max="8" id="week" name="week" value="<?= Wo_Secure((string) ($values['week'] ?? '')) ?>" required>
            </div>
            <div class="field">
                <label for="day">Day</label>
                <select id="day" name="day" required>
                    <option value="">Select day</option>
                    <?php foreach (['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'] as $day): ?>
                        <option value="<?= Wo_Secure($day) ?>" <?= (($values['day'] ?? '') === $day) ? 'selected' : '' ?>><?= Wo_Secure($day) ?></option>
                    <?php endforeach; ?>
                </select>
            </div>
            <div class="field full">
                <label for="title">Title</label>
                <input type="text" id="title" name="title" value="<?= Wo_Secure((string) ($values['title'] ?? '')) ?>" required>
            </div>
            <div class="field full">
                <label for="description">Description</label>
                <textarea id="description" name="description" required><?= Wo_Secure((string) ($values['description'] ?? '')) ?></textarea>
            </div>
            <div class="field full">
                <label for="success_criteria">Success Criteria</label>
                <textarea id="success_criteria" name="success_criteria" required><?= Wo_Secure((string) ($values['success_criteria'] ?? '')) ?></textarea>
            </div>
            <div class="field full">
                <label for="traps">Traps / Watch Outs</label>
                <textarea id="traps" name="traps"><?= Wo_Secure((string) ($values['traps'] ?? '')) ?></textarea>
            </div>
            <div class="field">
                <label for="event_date">Date</label>
                <input type="date" id="event_date" name="event_date" value="<?= Wo_Secure((string) ($values['event_date'] ?? '')) ?>" required>
            </div>
        </div>

        <div class="form-actions" style="margin-top: 18px;">
            <button class="btn" type="submit">Save Event</button>
            <a class="ghost-btn" href="index.php?link1=internship-calendar">Cancel</a>
        </div>
    </form>
</section>

