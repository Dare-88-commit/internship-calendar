<?php

declare(strict_types=1);

$wo = $wo ?? [];
$calendar = $wo['internship_calendar'] ?? [];
$event = $calendar['event'] ?? [];
?>

<section class="form-shell">
    <div class="empty-state">
        <span class="pill warning">Delete confirmation</span>
        <h1>Delete this event?</h1>
        <p>
            <strong><?= Wo_Secure((string) ($event['title'] ?? '')) ?></strong><br>
            Week <?= (int) ($event['week'] ?? 0) ?> • <?= Wo_Secure((string) ($event['day'] ?? '')) ?> • <?= Wo_Secure(app_format_date((string) ($event['event_date'] ?? ''))) ?>
        </p>

        <form method="post" class="form-actions" data-confirm="This will permanently delete the event. Continue?">
            <input type="hidden" name="id" value="<?= (int) ($event['id'] ?? 0) ?>">
            <button class="danger-btn" type="submit">Yes, delete it</button>
            <a class="ghost-btn" href="index.php?link1=internship-calendar-event&eid=<?= (int) ($event['id'] ?? 0) ?>">Cancel</a>
        </form>
    </div>
</section>

